SELECT SUM(CONTRATO.COSTO) AS INGRESO, OPERADOR.IDOPERADOR AS PROVEEDOR
FROM CONTRATO INNER JOIN OPERADOR ON CONTRATO.IDOPERADOR = OPERADOR.IDOPERADOR
WHERE TO_DATE(CONTRATO.FECHAINIC,'YYYY-MM-DD') BETWEEN ADD_MONTHS(TRUNC(SYSDATE),-12) AND SYSDATE
GROUP BY OPERADOR.IDOPERADOR;